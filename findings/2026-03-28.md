# AI Scout Findings — 2026-03-28

> ✅ Telegram digest delivered (AM + PM + evening sweep).

## Digest (AM sweep)

🔍 AI Scout — 2026-03-28

⚡ ACTION NEEDED (2)
1. [Playwright] — v1.57 switched from Chromium to Chrome for Testing builds. UI review route crawler screenshots may shift — re-baseline reference screens after upgrading.
2. [Playwright] — v1.58 removed `_react`, `_vue` selectors and `:light` CSS suffix. Verify route crawler doesn't use these before upgrading.

📌 BOOKMARKED (3)
3. [CC v2.1.86] — Fixed Write/Edit/Read failing on files outside project root with conditional skills.
4. [CC v2.1.86] — Fixed unnecessary config disk writes on every skill invocation causing perf issues.
5. [CC Ecosystem] — HN reports Claude uptime dropped below 99% in Q1 2026.

## Digest (PM sweep — supplemental)

💡 OPPORTUNITY (3)
6. CC v2.1.86 fixes memory leak in long sessions — render caches were never freed.
7. CC v2.1.86 restores editing ~/.claude/CLAUDE.md when conditional skills are active.
8. CC v2.1.86 fixes marketplace plugin "Permission denied" on macOS/Linux since v2.1.83.

📌 BOOKMARKED (1)
9. playwright-ghost — newer plugin-based alternative to ghost-cursor-playwright, actively maintained.

---

## Action Needed

- **Playwright 1.57 — Chromium → Chrome for Testing** (recent 2026 release)
  - Playwright now uses Chrome for Testing builds instead of Chromium for both headed and headless mode.
  - This changes the rendering engine used for screenshots. UI review route crawler baselines may produce different pixel output.
  - **Action:** After upgrading Playwright, re-capture reference screens and re-baseline visual comparisons. Expect minor rendering differences.
  - Affects: `project-audit.md` → Visual Verification, UI Review System (route crawler, capture sets, reference screens)

- **Playwright 1.58 — Removed deprecated selectors** (recent 2026 release)
  - Removed `_react` and `_vue` selectors entirely. Use locators guide alternatives.
  - Removed `:light` selector engine suffix. Use standard CSS selectors.
  - Removed `Devtools` option from `BrowserType.LaunchAsync()`.
  - Removed macOS 13 (Ventura) support for WebKit.
  - **Action:** Before upgrading, audit any Playwright scripts in projects for usage of `_react`, `_vue`, or `:light` selectors. Fix before upgrade.
  - Affects: `project-audit.md` → Visual Verification, UI Review route crawler

## Opportunities

- **CC v2.1.86 — Memory growth fix for long sessions** (March 27)
  - Markdown/highlight render caches were retaining full content strings, causing memory growth in long sessions.
  - Fixed in v2.1.86. Upgrade resolves session instability and potential OOM on long-running tasks.
  - Affects: All long-running CC sessions, scheduled tasks, extended development sessions.

- **CC v2.1.86 — Write/Edit/Read outside project root restored** (March 27)
  - Write/Edit/Read was failing on files outside the project root (e.g., `~/.claude/CLAUDE.md`) when conditional skills or rules are configured.
  - This was a regression introduced in v2.1.85. Fixed in v2.1.86.
  - Affects: Anyone editing global CLAUDE.md or cross-project files with conditional skills active.

- **CC v2.1.86 — Marketplace plugin permissions fix** (March 27)
  - Official marketplace plugin scripts were failing with "Permission denied" on macOS/Linux since v2.1.83.
  - Fixed in v2.1.86. Any plugins installed since v2.1.83 may need reinstallation or will work on upgrade.
  - Affects: All macOS/Linux users who installed marketplace plugins since March 25.

## Bookmarked

- **CC v2.1.86 key fixes** (March 27, 2026)
  - **Skills config corruption fix**: Unnecessary config disk writes on every skill invocation could cause performance issues and config corruption (Windows).
  - **Token overhead reduction**: `@` file mentions no longer JSON-escape raw string content, reducing token usage.
  - **`--resume` fix**: Sessions with pre-v2.1.85 history no longer fail with "tool_use ids were found without tool_result blocks."
  - **Prompt cache improvements**: Better cache hit rates for Bedrock, Vertex, and Foundry users.
  - **Memory optimizations**: Multiple cache management improvements reducing memory growth in long sessions.

- **Claude Uptime Q1 2026** — Hacker News discussion about Anthropic losing >99% uptime in Q1 2026. Could affect reliability of CC scheduled tasks. No official Anthropic response found. Monitor.

- **Playwright 1.56 — Playwright Agents** — New agent definitions (planner, generator, healer) for AI-assisted test building. Not immediately actionable but interesting for future test generation workflows.

- **Agents `initialPrompt` frontmatter** (CC v2.1.83, March 25)
  - Agents can now declare `initialPrompt` in frontmatter to auto-submit the first turn.
  - Could streamline agent/skill patterns — agents start working immediately without manual prompt.

- **playwright-ghost plugin system** (active in 2026)
  - `regseb/playwright-ghost` offers a plugin-based architecture for human-like browser automation with Playwright.
  - More actively maintained than `ghost-cursor-playwright`. Modular plugin system allows adding humanization behaviors (cursor movement, typing patterns) independently.
  - Relevant to demo recording pipeline if founder evaluates alternatives to ghost-cursor-playwright.

- **CC v2.1.86 — VSCode extension fixes** (March 27)
  - Fixed extension incorrectly showing "Not responding" during long operations.
  - Fixed extension defaulting Max plan users to Sonnet after OAuth token refreshes (8 hours after login) — silent model downgrade.
  - Relevant if founder uses VSCode extension with Max plan.

## Sources Checked

- ✅ CC Docs Changelog — https://code.claude.com/docs/en/changelog (fetched, v2.1.86 from March 27 detailed)
- ✅ Releasebot Anthropic — https://releasebot.io/updates/anthropic (fetched, no new releases since March 26)
- ✅ Anthropic Blog — searched `site:anthropic.com/research OR site:anthropic.com/news` (no new posts in 48h window)
- ✅ Hacker News AI — searched `site:news.ycombinator.com` (uptime discussion, subprocessor changes, OpenCode legal action — evaluated)
- ⚠️ Reddit r/ClaudeAI — searched multiple queries (no indexed results from last 48h; search engine indexing lag persists)
- ✅ Playwright Releases — no new release since v1.58; v1.57 Chrome switch and v1.58 removals confirmed
- ✅ ghost-cursor-playwright — no new releases; playwright-ghost noted as alternative
- ✅ Workflow/Pattern searches — 8 queries run (Claude Code workflows, multi-agent, hooks, MCP, skills, Remotion, solo dev, Cursor vs CC, frontend design)
- ✅ Remotion — repo active (updated March 28), Lambda SDKs updated March 24-26; no new major features in 48h
- ✅ Anthropic HN threads — uptime (bookmarked), subprocessor changes (enterprise noise), OpenCode legal (noise)

## Evaluation Notes

- **Playwright 1.57/1.58** categorized as ACTION NEEDED because they contain breaking changes that directly affect the UI review route crawler and visual verification pipeline documented in `project-audit.md`.
- **v2.1.86 bug fixes** elevated to OPPORTUNITY where they fix regressions affecting the founder's workflow (memory leak, file access outside project root, plugin permissions). These are "upgrade to fix" items, not template changes.
- **Workflow/pattern searches** returned mostly evergreen content. No new actionable discoveries in the 48h window.
- **Figma MCP** surfaced as a design-to-code bridge. Not flagged because the founder's design workflow uses JSX artifacts, not Figma.
- **Context7 MCP** (library docs server) surfaced multiple times. Useful convenience but not a workflow step reduction.
- **Remotion Skills** at 150K installs. Founder already uses Remotion for demo videos. No new capabilities in 48h.
- **Google Gemini Code Assist free** — noise per filter (founder committed to CC).
- **Tier 2 sources not checked** — today is Saturday. Tier 2 runs on Mondays only.

---

## Evening Sweep (supplemental)

### Digest (evening sweep)

🔍 AI Scout — 2026-03-28 (evening sweep)

⚡ ACTION NEEDED (1)
1. CC Channels now bridges Telegram natively — ops pipeline can drop Composio workaround

💡 OPPORTUNITY (1)
2. Claude Mythos leaked — next-gen tier above Opus, step-change in coding autonomy

📌 BOOKMARKED (2)
3. CC v2.1.85 adds MCP env vars and expanded conditional hooks
4. Skills/slash commands now support effort frontmatter

---

### Action Needed (evening)

- **CC Channels — Native Telegram Bridge** (research preview, launched March 20, 2026)
  - Claude Code now supports `--channels` flag that connects MCP servers as two-way message bridges into running sessions.
  - **Telegram and Discord are the launch platforms**, with iMessage added a week later.
  - How it works: start CC with `--channels`, a polling service monitors the Telegram plugin. Messages from paired Telegram users are injected into the active session as `<channel>` events. CC can execute code, run tests, and reply back through the same channel.
  - Three-layer security: sender allowlist (paired Telegram user IDs only), per-session opt-in (`--channels` flag required every launch), plugin whitelisting (only Anthropic-approved plugins).
  - Requires CC v2.1.80+, claude.ai login (not API keys). Team/Enterprise must explicitly enable via admin settings.
  - **Fakechat** demo included for local testing before connecting external platforms.
  - **Impact on founder's workflow:** This directly replaces Phase 8 ops pipeline's Composio MCP workaround for Telegram. Native two-way Telegram bridge means CC can both receive commands from Telegram (approve deployments, trigger tasks) AND send reports back — without Composio or manual curl calls.
  - **Action:** Evaluate CC Channels as replacement for the current Telegram integration in ops pipeline. Update `new-project-master.md` Phase 8 if adopted. Test with Fakechat first.
  - Affects: `new-project-master.md` → Phase 8 (Ops Pipeline), `CLAUDE.md` → CC Channels/permission relay section
  - Docs: https://code.claude.com/docs/en/channels

### Opportunities (evening)

- **Claude Mythos — Leaked Next-Gen Model** (March 27, 2026)
  - Anthropic accidentally exposed a draft blog post and ~3,000 unpublished assets via an unsecured public content cache.
  - Mythos is a new tier above Opus — the model hierarchy is now Haiku → Sonnet → Opus → Mythos (internal codename "Capybara").
  - Leaked benchmarks show "dramatically higher scores" in software coding, academic reasoning, and cybersecurity compared to Opus 4.6.
  - Anthropic confirmed development and testing but noted it is very expensive to serve. No general release planned — rollout is invite-only, with cyber defenders getting early access first.
  - Cybersecurity stocks dropped 3-6% on the news due to fears about vulnerability exploitation speed.
  - **Impact on founder's workflow:** When available, a step-change in coding autonomy could enable patterns currently not practical — more complex autonomous CC workflows, potentially the inline evaluator pattern. However, "very expensive" pricing works against the founder's cost-conscious approach.
  - **What to watch:** Availability timeline, pricing, and whether CC supports model selection for Mythos tier. The founder should track when/if Mythos becomes available through Max plan or at reasonable API pricing.
  - Also notable: Anthropic is considering an IPO as soon as October 2026 (Bloomberg) — platform stability signal.

### Bookmarked (evening)

- **CC v2.1.85 — MCP Environment Variables and Conditional Hooks** (March 26, 2026)
  - MCP servers can now receive environment variables configured in `.mcp.json`, making it easier to pass secrets and config to MCP integrations.
  - Conditional hooks expanded — the founder already uses `if` field filtering on git-guard and auto-format hooks. This update may improve matching or add new condition types.
  - Also in v2.1.85: expanded deep link query support to 5,000 characters, improved scroll performance (WASM yoga-layout replaced with pure TypeScript).

- **Effort Frontmatter for Skills and Slash Commands** (CC v2.1.84-86 range)
  - Skills can now declare an `effort` level in their YAML frontmatter, controlling the reasoning effort Claude applies when executing the skill.
  - Relevant to the self-growing skills architecture — skills like `error-triage.md` or `visual-qa/SKILL.md` could declare appropriate effort levels (e.g., high effort for visual QA, lower for routine health checks).
  - Also: `source: 'settings'` plugin marketplace source allows declaring plugin entries inline in settings.json.

### Sources Checked (evening)

- ✅ CC Docs Changelog — https://code.claude.com/docs/en/changelog (v2.1.86 confirmed, no new releases since AM sweep)
- ✅ Releasebot Anthropic — https://releasebot.io/updates/anthropic (v2.1.85/v2.1.84 from March 26 detailed)
- ✅ Anthropic Blog/News — Economic Index March 2026 report (not workflow-relevant), no new product announcements in 48h
- ✅ Hacker News — Claude Mythos leak (Fortune, CoinDesk, Bloomberg), Anthropic subprocessor changes (enterprise noise), uptime Q1 (already bookmarked AM)
- ⚠️ Reddit r/ClaudeAI — no indexed results from last 48h (search engine indexing lag persists)
- ✅ Playwright — no new releases since v1.58 (already covered AM)
- ✅ Workflow/pattern searches — CC Channels Telegram integration surfaced as major missed finding from March 20
- ✅ Remotion — no new developments since AM sweep
- ✅ Claude Code plugins/skills searches — effort frontmatter and settings plugin source noted
- ✅ Solo dev / AI tools searches — no new tools matching founder's filter in 48h window

### Evaluation Notes (evening)

- **CC Channels** is the biggest catch of this sweep — it launched March 20 but was missed by prior sweeps. It directly addresses the Phase 8 ops pipeline's Telegram integration and is explicitly in the CLAUDE.md watch list. Categorized ACTION NEEDED because it may obsolete the Composio workaround.
- **Claude Mythos** is categorized as OPPORTUNITY, not ACTION NEEDED, because it's invite-only with no general availability timeline. However, it's the most significant Anthropic announcement in months and directly affects what CC can do autonomously in the future.
- **CC v2.1.85 changes** are BOOKMARKED because they improve existing systems (hooks, MCP) but don't require immediate template changes.
- **Effort frontmatter** is a nice-to-have for the skills architecture but not urgent.
- Skipped as noise: Anthropic IPO plans (business news), Claude March usage promotion, Anthropic subprocessor changes (enterprise), AI tool comparison lists (evergreen), beginner tutorials, Gemini Code Assist free tier (competitor noise per filter), "awesome claude code" link dumps.
