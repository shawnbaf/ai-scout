# AI Scout Findings — 2026-04-06

> Monday sweep — Tier 1 + Tier 2 sources checked.

## Action Needed

### 1. Axios npm Supply Chain Attack — RAT Deployed via Compromised Maintainer Account

On March 30, 2026, the axios npm package (100M+ weekly downloads) was compromised. Attacker hijacked the npm account of lead maintainer Jason Saayman via targeted social engineering by North Korean state actor UNC1069 (tracked by Microsoft as Sapphire Sleet). Two poisoned versions published:

- **axios@1.14.1** (tagged `latest`) — malicious
- **axios@0.30.4** (tagged `legacy`) — malicious
- **Safe versions:** axios@1.14.0 and axios@0.30.3

The attack was subtle: a single dependency (`plain-crypto-js`) added to package.json with a `postinstall` hook that silently downloaded a cross-platform RAT (macOS, Windows, Linux). The malicious versions were live for approximately 3 hours before npm removed them.

**Why this matters to us:** axios is likely in the transitive dependency tree of Next.js/Supabase/Vercel projects. Anyone who ran `npm install` during the 3-hour window on March 30-31 could have installed the compromised version. CI/CD pipelines are particularly at risk.

**Cross-system impact:**
- **LAUNCH-CHECKLIST.md:** Should add `npm audit` / `npx lockfile-lint` as a pre-launch verification step.
- **Health System (Track 4):** Consider adding periodic `npm audit` to the health check workflow.
- **CLAUDE.md Hard Rules:** Consider adding "Run `npm audit` before committing dependency changes" as a hard rule.
- **project-audit.md:** Should include dependency audit checkpoint.

**Action:** Run `npm ls axios` in all projects to verify no compromised versions were installed. Check `package-lock.json` for any reference to `plain-crypto-js`. Run `npm audit` across all projects.

Sources: elastic.co/security-labs/axios-one-rat-to-rule-them-all, microsoft.com/en-us/security/blog/2026/04/01/mitigating-the-axios-npm-supply-chain-compromise, aikido.dev/blog/axios-npm-compromised-maintainer-hijacked-rat

### 2. Next.js CVE-2026-27979 (DoS) + CVE-2026-29057 (HTTP Request Smuggling)

Two security vulnerabilities disclosed in Next.js affecting versions 16.0.1 through 16.1.6:

**CVE-2026-27979 — Denial of Service via PPR:**
- Requests containing the `next-resume: 1` header cause unbounded memory consumption in non-minimal deployment configurations.
- Exploits Partial Prerendering (PPR) feature. No authentication required.
- **Fixed in Next.js 16.1.7+** (enforces `maxPostponedStateSize` limits).

**CVE-2026-29057 — HTTP Request Smuggling:**
- Remote attacker can perform HTTP request smuggling via crafted DELETE/OPTIONS requests using `Transfer-Encoding: chunked`.
- Conditional exploitation: requires Next.js rewrites pointing to an external backend.
- Patches the embedded http-proxy.

**Cross-system impact:**
- **new-project-master.md Phase 2 (Architecture):** Projects using PPR need to be on 16.1.7+. New projects should start on 16.2.
- **project-audit.md:** Add Next.js version check against known CVEs.
- **LAUNCH-CHECKLIST.md:** Add minimum Next.js version verification.
- **Health System (Track 4):** Could add version check to health CLI.

**Action:** Verify all projects are on Next.js 16.1.7+ or 16.2. If using PPR, this is urgent.

Sources: herodevs.com/blog-posts/cve-2026-29057-and-cve-2026-27980, nvd.nist.gov/vuln/detail/CVE-2026-27979, sentinelone.com/vulnerability-database/cve-2026-27979

### 3. CVE-2026-23864 — React/Next.js DoS via Server Components Memory Exhaustion

Denial-of-service vulnerability in React Server Components (CVSS 7.5 High). A specially crafted HTTP request to any Server Function endpoint causes memory exhaustion or excessive CPU, crashing the server. **No authentication required.**

**Affected versions:** React 19.0.x through 19.2.x, Next.js 15.x through 16.x.
**Patched versions:** React 19.0.4, 19.1.5, 19.2.4.

**Why this matters:** Directly affects every Next.js App Router project using Server Components — which is all projects built with the founder's template. An unauthenticated attacker can take down production.

**Cross-system impact:**
- **new-project-master.md Phase 2:** All new projects must start on patched React versions.
- **project-audit.md:** Add React version check against CVE-2026-23864.
- **LAUNCH-CHECKLIST.md:** Add minimum React patch version verification.
- **Health System (Track 4):** Version check should be part of health CLI output.

**Action:** Run `npm ls react` in all projects. Verify React is on 19.0.4+, 19.1.5+, or 19.2.4+.

Sources: akamai.com/blog/security-research/cve-2026-23864, vercel.com/changelog/summary-of-cve-2026-23864

### 4. Shai-Hulud Worm — @ctrl/tinycolor and 500+ npm Packages Compromised

Self-propagating supply chain worm that compromised @ctrl/tinycolor (2.2M weekly downloads) and spread to 500+ packages including CrowdStrike packages, angulartics2, and various @ctrl and @nativescript-community packages. Steals GITHUB_TOKEN, NPM_TOKEN, AWS keys, and attempts cloud metadata discovery in CI/CD environments.

**Why this matters:** The worm specifically targets CI/CD environments and cloud build agents (including Vercel builds). Any compromised package can infect packages maintained by the same author — it's self-replicating.

**Cross-system impact:**
- **LAUNCH-CHECKLIST.md:** Add @ctrl namespace audit as a pre-launch step.
- **Health System (Track 4):** `npm audit` should be standard in health checks.

**Action:** Run `npm ls | grep @ctrl` in all projects. Pin exact versions in package-lock.json. Use `npm audit` for ongoing monitoring.

Sources: socket.dev/blog/tinycolor-supply-chain-attack-affects-40-packages, unit42.paloaltonetworks.com/npm-supply-chain-attack

## Opportunities

### 5. Next.js 16.2 Agent DevTools — First-Class AI Agent Support

Next.js 16.2 (released March 18, 2026) treats AI coding agents as first-class users of the framework with three significant additions:

**AGENTS.md in create-next-app:**
- New projects generated with `create-next-app` now include an `AGENTS.md` file containing version-matched Next.js documentation.
- Research showed agents with bundled docs achieved a **100% pass rate** on Next.js evals vs. 79% max with skill-based approaches.
- This is directly relevant to how the founder bootstraps projects — the new-project-master.md Phase 2 should recommend starting with 16.2's create-next-app to get AGENTS.md automatically.

**Experimental Agent DevTools (`next-browser`):**
- CLI that exposes browser-level data to terminal agents: screenshots, network requests, console logs, component trees, props, hooks, PPR shells, errors.
- Invoked as `/next-browser` in CC, Cursor, or any agent supporting skills.
- This is a significant upgrade for the UI Review system — CC could inspect component state and props during visual verification, not just screenshots.

**Browser Error Forwarding:**
- Client-side errors now forwarded to terminal by default during development.
- CC sees browser errors without needing to switch to browser console.
- Directly improves CC's ability to diagnose frontend issues autonomously.

**Cross-system impact:**
- **new-project-master.md Phase 2:** Recommend Next.js 16.2+ for all new projects. Note AGENTS.md as part of the bootstrap.
- **UI Review (Track 5):** `next-browser` could supplement or replace parts of the Playwright route crawler for component-level inspection.
- **Health System (Track 4):** Browser error forwarding means CC catches frontend errors during health checks without extra tooling.
- **CLAUDE.md Template:** Could add `/next-browser` as an available skill. AGENTS.md reduces the need for framework-specific instructions in CLAUDE.md.
- **Demo Navigator (Phase 9):** `next-browser` provides component trees useful for debugging demo interactions.

Additionally, Vercel released **next-devtools-mcp** (`github.com/vercel/next-devtools-mcp`, 707 stars) — an official MCP server installable via `claude mcp add next-devtools npx next-devtools-mcp@latest`. Gives CC real-time access to build/runtime errors, routes, component metadata, server logs, automated Next.js 16 upgrades with codemods, and official docs search.

Source: nextjs.org/blog/next-16-2-ai, github.com/vercel/next-devtools-mcp

### 4. TurboQuant llama.cpp Implementation — KV Cache Compression Ready for Review

TurboQuant (Zandieh et al., ICLR 2026) now has a working implementation for llama.cpp with CPU and CUDA kernels:

**Implementation status:**
- CPU implementation: all components in C, no dependencies, 18/18 tests passing, MSE matching paper within 1%
- CUDA kernels: GPU quantize/dequant and fused attention dot product
- TQ3 (3-bit): MSE = 0.034 with **4.9x compression** vs FP16
- TQ4 (4-bit): MSE = 0.009 with **3.8x compression** vs FP16

**How it works:** Two-stage compression pipeline — random orthogonal rotation spreads energy uniformly across KV vector coordinates, then Lloyd-Max algorithm computes optimal quantization buckets. No training data, calibration, or model-specific tuning needed. PolarQuant (AISTATS 2026) is a complementary approach using polar coordinates instead of Cartesian, eliminating per-block normalization constants.

**Community adoption:** TurboQuant+ community implementation adds turbo3 and turbo4 KV cache compression to llama.cpp, enabling massive context windows on Apple Silicon and consumer GPUs. The PR is under review for upstream llama.cpp integration.

**Cross-system impact:**
- **Local model routing (Alienware 8GB VRAM):** This is the breakthrough the founder has been explicitly monitoring. TQ3 at 4.9x compression would allow running significantly larger context windows within the existing VRAM envelope. A 7B model that currently fits 4K context could potentially handle 16-20K context.
- **Health System (Track 4):** Expanded local context could enable local model-based code review or analysis tasks that were previously too context-limited.

**Action (when merged):** Monitor the llama.cpp PR. Once merged upstream and available in Ollama, test with the Alienware's GPU configuration.

Sources: github.com/ggml-org/llama.cpp/discussions/20969, research.google/blog/turboquant-redefining-ai-efficiency-with-extreme-compression, github.com/TheTom/turboquant_plus

### 5. claude-worktree-hooks — Automated Env/Deps/Ports for Parallel Worktrees

Drop-in hook scripts (`github.com/tfriedel/claude-worktree-hooks`) that solve the biggest friction point in parallel CC sessions: empty worktrees. When you run `claude --worktree`, the WorktreeCreate hook auto-copies `.env` files, installs dependencies, and assigns deterministic ports (MD5 hash of branch name, range 3100-9999). WorktreeRemove hook kills processes on that port and cleans up.

**Why it matters:** Running parallel CC sessions via `--worktree` currently means each worktree starts with no `.env`, no `node_modules`, and potential port collisions. This eliminates the manual setup step entirely. Deterministic ports mean no collisions between agents.

**Cross-system impact:**
- **new-project-master.md:** Multi-agent orchestration pattern gets a drop-in solution for worktree setup.
- **CLAUDE.md Template:** Could document these hooks in the Hooks section as a standard addition.
- **Ops Pipeline (Phase 8):** Parallel track workflows become more reliable.

Source: github.com/tfriedel/claude-worktree-hooks

### 6. Context-Mode Plugin — 98% Context Window Savings via MCP Sandbox

MCP server plugin (`github.com/mksglu/context-mode`) that sandboxes tool output to dramatically reduce context consumption. Playwright snapshots: 56KB to 299B. GitHub issues: 59KB to 1.1KB. Session time before slowdown goes from ~30min to ~3hrs. Also tracks edits/git/tasks in SQLite with BM25 search for session continuity after compaction.

**Why it matters:** Every long-running CC session hits context limits — health system runs, UI review route crawling, codebase audits, and this scout itself. This plugin would extend session life by 6x. Install: `/plugin marketplace add mksglu/context-mode`.

**Cross-system impact:**
- **Health System (Track 4):** Runs longer without degradation.
- **UI Review (Track 5):** Playwright snapshots compress dramatically during route crawling.
- **This Scout:** Longer sessions mean more sources checked per run without context pressure.
- **Scheduled Tasks:** All autonomous sessions benefit from extended context life.

Source: github.com/mksglu/context-mode

### 7. code-review-graph — Local Knowledge Graph for Targeted Code Review

MCP server (`github.com/tirth8205/code-review-graph`) that parses codebases with Tree-sitter into a SQLite graph (functions, classes, imports, call edges). On review requests, computes "blast radius" — minimal affected file set. 27,732 files narrowed to ~15 files. 6.8-49x token reduction. Incremental updates via SHA-256 hash checks in under 2 seconds.

**Why it matters:** The founder's `project-audit.md` runs full codebase audits. This MCP server would make CC read only what matters during audits instead of scanning everything. Install: `pip install code-review-graph && code-review-graph install`.

**Cross-system impact:**
- **project-audit.md:** Audits become dramatically cheaper and faster.
- **Health System (Track 4):** Targeted code quality checks instead of full scans.
- **Fix Guardrails skill:** Blast radius computation aligns with the pre-change verification pattern.

Source: github.com/tirth8205/code-review-graph

### 8. Impeccable — Design Vocabulary Upgrade for Visual QA

Paul Bakaus's skill (`github.com/pbakaus/impeccable`, 10K+ GitHub stars) that layers on top of Anthropic's official frontend-design skill. Adds 20 steering commands (/audit, /polish, /critique), 7 domain-specific design reference files, and curated anti-patterns. Third-party benchmarks show 1.59x quality improvement over base skill.

**Why it matters:** The founder's `enhancement-standards.md` and `visual-qa/SKILL.md` define design quality gates. Impeccable provides the exact vocabulary CC needs to hit those gates consistently — anti-patterns that prevent the "Inter + purple gradient" convergence that plagues AI-generated UIs. Install: `npx skills add pbakaus/impeccable`.

**Cross-system impact:**
- **Screen Design (Phase 6.5):** More distinctive, higher-quality outputs from CC.
- **UI Review (Track 5):** Pass 2 enhancement suggestions get sharper critique vocabulary.
- **Demo Video (Phase 10):** Visual quality of recorded screens improves.
- **enhancement-standards.md:** Could cross-reference Impeccable's anti-patterns.

Source: github.com/pbakaus/impeccable, impeccable.style

### 9. Supabase "Secure by Default" Changes for 2026

Supabase has shipped and is shipping new security defaults:
- **RLS enabled by default** on all tables created via dashboard
- **pg_graphql disabled by default** (reduces attack surface)
- **Data API can be disabled entirely** at project creation
- **OpenAPI spec access via anon key deprecated** (March 11) — schema endpoint now requires service role key

**Why it matters:** The founder's `new-project-master.md` template should note these defaults. For existing projects, the OpenAPI deprecation could break tooling that reads the schema via anon key.

**Cross-system impact:**
- **new-project-master.md Phase 2:** Update architecture recommendation to note new Supabase defaults.
- **project-audit.md:** Add check for RLS enabled on all tables, OpenAPI access pattern verification.

Source: supabase.com/blog/supabase-security-2025-retro

### 10. OWASP Top 10 for Agentic Applications 2026

The 2026 edition of the OWASP Agentic Top 10 is published. Most relevant risks to the founder's CC-driven workflow:
- **ASI01 Agent Goal Hijack** — prompt injection redirecting agent goals (directly relevant to CC deny rules bypass from April 4 findings)
- **ASI02 Unsafe Tool Use** — agents misusing legitimate tools when misled
- **ASI05 Memory Poisoning** — poisoned documents rewriting agent rules (relevant to CLAUDE.md injection attacks)
- **ASI06 Multi-Agent Interaction Failures** — unauthenticated messages between agents (relevant to CC subagent orchestration)
- **ASI07 Supply Chain Compromise** — vulnerable tools/plugins used by agents

Key principles: **Least Agency** and **Strong Observability**. Validates the "human-in-the-loop" pattern from the Defer hook.

**Cross-system impact:**
- **CLAUDE.md Template:** ASI01/ASI05 validate the deny rules and CLAUDE.md sanitization practices.
- **Hooks:** The Defer hook (v2.1.89) implements the "Least Agency" principle for sensitive operations.

Source: genai.owasp.org/resource/owasp-top-10-for-agentic-applications-for-2026

## Bookmarked

### 11. Ollama Gemma 4 MoE — 26B Quality at 4B Active Parameters

Ollama v0.20.0-v0.20.2 (April 2-4) added support for Google's Gemma 4 model family (Apache 2.0 license). The 26B-A4B MoE variant has 26B total parameters but only 4B active during inference — meaning it could potentially fit on the Alienware's 8GB VRAM while delivering 26B-level quality. Run via `ollama run gemma4:26b-e4b`.

**Why bookmarked:** Needs testing on the actual hardware. If it fits in 8GB VRAM with acceptable speed, this immediately becomes the best local model option. Combined with TurboQuant (finding #6 in opportunities), a Gemma 4 MoE + TQ3 compression could deliver strong local inference with extended context.

Source: github.com/ollama/ollama/releases/tag/v0.20.0

### 12. Remotion @remotion/elevenlabs Captions Package + Audio Waveform Timeline

Recent Remotion releases (latest v4.0.445) include:
- **@remotion/elevenlabs:** New package for converting ElevenLabs output to captions — useful if the demo video pipeline ever adds narration.
- **Audio waveform in video timeline layers:** Studio now shows waveforms in the timeline, making it easier to sync video with audio/music.
- **Series as Sequence with `layout="none"`:** Simplifies composition structure.
- **10MB LRU cache for timeline video frames:** Studio performance improvement.

**Why bookmarked:** Not immediately actionable since the demo video pipeline doesn't currently use ElevenLabs or narration. But the captions package could enable auto-captioned demo videos in the future — a meaningful quality upgrade for accessibility and social media (where most videos play muted).

Source: github.com/remotion-dev/remotion/releases

### 13. 1M Context Window Beta Retiring April 30 for Sonnet 4.5/4

Anthropic is retiring the `context-1m-2025-08-07` beta header for Claude Sonnet 4.5 and Claude Sonnet 4 on **April 30, 2026**. After that date, requests exceeding the standard 200k-token context window on these models will return an error.

**Why bookmarked:** The founder is on Opus 4.6, which supports 1M context natively at standard pricing with no beta header. No action needed. But worth knowing in case any API integrations or scripts still reference older Sonnet models with the beta header.

Source: releasebot.io/updates/anthropic, thenewstack.io/claude-million-token-pricing

### 14. Vibe Coding Vulnerability Class — Scale Confirmed

Multiple large-scale studies published:
- **20% of vibe-coded apps** have security vulnerabilities (Wiz Research)
- **80% of vibe-coded apps** have broken RLS (multiple sources)
- **Moltbook breach (Jan 2026):** AI social network exposed 1.5M API tokens and 35K emails — RLS never enabled
- Common AI-generated RLS error: `auth.role() = 'authenticated'` instead of `auth.uid() = user_id`

**Why bookmarked:** The founder's template already enforces RLS patterns, but the specific `auth.role()` vs `auth.uid()` antipattern should be added as a Learned Correction in CLAUDE.md templates. Validates adding an explicit RLS policy audit step to `project-audit.md`.

Sources: wiz.io/blog/common-security-risks-in-vibe-coded-apps, blog.ogwilliam.com/post/moltbook-hack-supabase-vibe-coding

### 15. chalk/debug npm Compromise — 2B Weekly Downloads Affected

Attacker phished npm maintainer "qix" and published malicious versions of `chalk`, `debug`, `ansi-styles`, and 15 other packages (collectively 2B weekly downloads). Payload was a crypto-stealer targeting browser environments. Malicious versions live ~2 hours.

**Why bookmarked:** Both `chalk` and `debug` are deep transitive dependencies in virtually every Node.js project. Caught quickly, but reinforces the need for lockfile pinning and supply chain monitoring. Three major npm supply chain attacks in one month (axios, @ctrl/tinycolor, chalk/debug) suggests this attack vector is accelerating.

Sources: wiz.io/blog/widespread-npm-supply-chain-attack, paloaltonetworks.com/blog/cloud-security/npm-supply-chain-attack

## Sources Checked

### Tier 1 (Daily)
- CC GitHub CHANGELOG.md — No new releases since v2.1.92 (April 4)
- CC Docs Changelog (code.claude.com/docs/en/changelog) — Confirmed, no updates since April 4
- Releasebot (releasebot.io/updates/anthropic) — v2.1.92 still latest; also tracked 1M context retirement
- Anthropic Blog/News — OpenClaw subscription block (noise for us), AnthroPAC filing (noise), Coefficient Bio acquisition (noise)
- Hacker News — OpenClaw ban discussion trending, no new CC workflow discoveries
- Reddit r/ClaudeAI — No indexable content from April 4-6 window
- Playwright releases — No new releases since v1.59.0/v1.59.1 (reported April 4)
- Workflow pattern searches (15 queries) — Found claude-worktree-hooks, Context-Mode plugin, code-review-graph MCP, Impeccable design skill. Skipped: Boris Cherny parallel pattern (already known), generic plan-before-code advice, JSON orchestration frameworks (overhead for solo dev), Cursor hybrid workflows (committed to CC), SaaS video generators (not CLI-driven)

### Tier 2 (Monday)
- Next.js releases — **16.2 with Agent DevTools** (released March 18, evaluated today); **CVE-2026-27979 + CVE-2026-29057** patched in 16.1.7
- React ecosystem — React Compiler v1.0 stable since Oct 2025, no new releases. React 19.2.1 is latest (Dec 2025).
- Remotion releases — v4.0.445, @remotion/elevenlabs captions package, audio waveform timeline
- Ollama/llama.cpp — Ollama v0.20.0-v0.20.2 (April 2-4): **Gemma 4 MoE support** (26B-A4B, could fit 8GB VRAM), MLX engine for Apple Silicon (not relevant to Alienware); llama.cpp b8664 (April 4, routine build)
- TurboQuant/PolarQuant — **Working llama.cpp implementation under review** with CPU+CUDA kernels
- Composio MCP — No new updates found
- npm supply chain — **Axios compromised March 30** (North Korean state actor, RAT); **Shai-Hulud worm** (@ctrl/tinycolor, 500+ packages); **chalk/debug compromise** (2B weekly downloads)
- Next.js security — CVE-2026-27979 (DoS via PPR) + CVE-2026-29057 (request smuggling), patched in 16.1.7; **CVE-2026-23864** (React Server Components DoS, CVSS 7.5), patched in React 19.0.4/19.1.5/19.2.4
- Supabase security — **Secure-by-default changes** shipping (RLS default, pg_graphql disabled, OpenAPI anon key deprecated)
- OWASP GenAI — **Agentic Top 10 2026 published** (ASI01 Goal Hijack, ASI05 Memory Poisoning most relevant)
- Vibe coding security — **Scale confirmed** by Wiz Research (20% vuln rate), specific `auth.role()` antipattern identified
- aikido.dev/blog — Found n8n RCE (CVE-2026-21858), Storybook WebSocket hijacking (CVE-2026-27148); neither in founder's stack
- socket.dev/blog — Covered axios, @ctrl/tinycolor, chalk/debug attacks
- vercel.com/kb — CVE-2026-23864 coverage confirmed

## Evaluation Notes

- **The axios attack is the highest-priority security finding.** Even though the malicious versions were live for only 3 hours, axios has 100M+ weekly downloads. Any CI/CD pipeline that ran `npm install` during the window could be compromised. The attack vector (postinstall hook in a new dependency) bypasses all code review — you'd only catch it by auditing package.json changes or running `npm audit`.
- **Next.js 16.2 Agent DevTools is the most impactful opportunity.** AGENTS.md achieving 100% eval pass rate vs 79% for skill-based approaches is a strong signal. This could simplify the CLAUDE.md template for Next.js projects — instead of extensive framework-specific instructions, let AGENTS.md handle it. The `next-browser` experimental skill is worth testing as a supplement to the UI Review route crawler.
- **TurboQuant is the local model breakthrough the founder has been watching for.** The implementation is real (18/18 tests, MSE matches paper), not just a research paper. The PR is under review for llama.cpp upstream. Once merged and propagated to Ollama, this immediately expands what the Alienware can do.
- **OpenClaw subscription block is noise for us** — the founder uses CC directly, not through third-party harnesses. But it signals Anthropic is getting aggressive about third-party tool usage on subscription plans. Worth monitoring in case it affects CC plugins or MCP integrations in the future.
- **No new CC releases this weekend.** v2.1.92 (April 4) remains latest. The KAIROS teaser window (April 1-7 per the source leak) hasn't produced any visible feature flags yet — may be limited to Anthropic employees as predicted.
- **Context-Mode and code-review-graph are the most interesting MCP tools this cycle.** Context-Mode's 98% compression on Playwright snapshots directly addresses the UI Review route crawler's context hunger. code-review-graph's blast radius computation would make project audits dramatically cheaper. Both are install-and-go MCP servers.
- **Impeccable fills a specific gap in the visual QA pipeline.** The founder's enhancement-standards.md defines what "good" looks like, but CC still defaults to generic aesthetic choices. Impeccable's anti-pattern library and steering commands give CC the vocabulary to avoid the "Inter + purple gradient" convergence.
- **claude-worktree-hooks solves a real friction point.** Every `--worktree` session currently starts broken (no .env, no node_modules, random port). These hooks are drop-in shell scripts that eliminate the setup step entirely.
- **Filtered out:** Ollama v0.20.2 (UI change, no workflow impact), React ecosystem (no new releases), Composio MCP (no updates), OWASP (no updates), general AI productivity content, beginner tutorials, OpenClaw migration guides, Boris Cherny parallel pattern (already known), JSON orchestration frameworks (overhead), Cursor hybrid workflows (committed to CC), SaaS video generators (not CLI-driven), awesome-claude-code-toolkit (bulk collection, cherry-picked useful items), SkillsMP marketplace (link dump).
